<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/Reminder"
            android:checked="false"
            android:title="@string/reminder_string"
            android:icon="@drawable/icon_reminder"/>

        <item
            android:id="@+id/todoTasks"
            android:checked="false"
            android:title="@string/todo_string"
            android:icon="@drawable/ic_action_edit_dark"/>

        <item
            android:id="@+id/signOut"
            android:checked="false"
            android:title="@string/signOut_string"
            android:icon="@drawable/sign_out"/>

    </group>
</menu>